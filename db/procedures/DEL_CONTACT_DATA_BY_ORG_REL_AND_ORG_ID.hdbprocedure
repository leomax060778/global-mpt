PROCEDURE "MKTG_PLANNING_TOOL"."mktgplanningtool.db.procedures::DEL_CONTACT_DATA_BY_ORG_REL_AND_ORG_ID"
(   IN in_organization_related_id BIGINT,
    IN in_organization_id BIGINT,
    OUT out_result BIGINT
)
LANGUAGE SQLSCRIPT
SQL SECURITY INVOKER
DEFAULT SCHEMA "MKTG_PLANNING_TOOL"
AS
BEGIN

DELETE FROM INTERLOCK_ORGANIZATION_CONTACT_DATA
WHERE ORGANIZATION_RELATED_ID = in_organization_related_id
AND ORGANIZATION_ID = in_organization_id;

  SELECT ::ROWCOUNT INTO out_result FROM DUMMY;
END;
