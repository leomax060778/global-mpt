PROCEDURE "MKTG_PLANNING_TOOL"."mktgplanningtool.db.procedures::GET_ALL_SEGMENTATION_SALES"(
		OUT out_result TABLE (
			SEGMENTATION_SALES_ID BIGINT,
			NAME NVARCHAR(255),
			PARENT_ID BIGINT
		)
	)
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER
	DEFAULT SCHEMA "MKTG_PLANNING_TOOL"
	READS SQL DATA
AS
BEGIN
	out_result = SELECT SEGMENTATION_SALES_ID,
				NAME, PARENT_ID
			FROM SEGMENTATION_SALES
			WHERE ENABLED = 1
				AND DELETED = 0
			ORDER BY NAME ASC;
END;
