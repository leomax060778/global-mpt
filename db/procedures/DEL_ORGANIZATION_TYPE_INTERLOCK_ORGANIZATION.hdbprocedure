PROCEDURE "MKTG_PLANNING_TOOL"."mktgplanningtool.db.procedures::DEL_ORGANIZATION_TYPE_INTERLOCK_ORGANIZATION" (
	IN in_organization_type_interlock_organization_id bigint,
	IN in_user_id bigint,
	OUT out_result bigint
)
	
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER
	DEFAULT SCHEMA "MKTG_PLANNING_TOOL"
	AS
BEGIN

	UPDATE ORGANIZATION_TYPE_INTERLOCK_ORGANIZATION
	SET
		deleted = 1,
	    enabled = 0,
	    modified_user_id = in_user_id,
	    modified_date_tz = CURRENT_TIMESTAMP
    WHERE organization_type_interlock_organization_id = in_organization_type_interlock_organization_id;

	SELECT ::ROWCOUNT INTO out_result FROM DUMMY;
END;
