PROCEDURE "MKTG_PLANNING_TOOL"."mktgplanningtool.db.procedures::GET_PLAN_BY_USER" ( 
	IN in_filter NVARCHAR(5000)
	, OUT out_result TABLE(
	  HL2_ID BIGINT
	  , USER_ID BIGINT
	)
) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER 
	DEFAULT SCHEMA "MKTG_PLANNING_TOOL"
	READS SQL DATA AS
BEGIN

	out_result = 
	APPLY_FILTER("_SYS_BIC"."mktgplanningtool.db.data.views/CV_PLAN_USER", :in_filter);

END;
